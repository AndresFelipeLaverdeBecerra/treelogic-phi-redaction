# Pesos del modelo

Esta carpeta está reservada para los pesos del detector durante la ejecución local.

Archivo esperado:

    models/best.pt

El repositorio público contiene el código, la configuración y la documentación del pipeline. Los pesos del modelo se distribuyen únicamente por un canal de evaluación autorizado cuando corresponda.
